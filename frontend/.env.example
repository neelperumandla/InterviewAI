# Local dev: Vite proxies /ws to the API (see vite.config.ts)
# VITE_API_PORT=8001

# Production (Vercel): public Railway WebSocket URL — must use wss://
# VITE_WS_URL=wss://your-service.up.railway.app/ws
